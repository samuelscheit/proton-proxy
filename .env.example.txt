# ProtonVPN OpenVPN/IKEv2 credentials. These are not your normal Proton login
# and are only required when at least one selected profile uses OpenVPN.
PVPN_USERNAME=YOUR_PROTONVPN_OPENVPN_USERNAME
PVPN_PASSWORD=YOUR_PROTONVPN_OPENVPN_PASSWORD

# Used only when OpenVPN credentials are reset after AUTH_FAILED, or when
# RESET_CREDENTIALS_ON_START=true.
PROTON_USERNAME=YOUR_PROTON_ACCOUNT_USERNAME
PROTON_PASSWORD=YOUR_PROTON_ACCOUNT_PASSWORD

# 0 uses every supported .ovpn/.conf profile in /etc/openvpn/configs; otherwise
# selects the first N profiles sorted by filename. OpenVPN uses tunN and
# WireGuard uses wgN interfaces.
MAX_CONNECTIONS=0
BASE_PROXY_PORT=8100
PORT_GAP=1

# Keep the fail-closed IPv4 guard enabled.
REQUIRE_TUN_IP=true
CONNECT_TIMEOUT_MS=30000
TUN_IP_WAIT_MS=30000
STARTUP_TIMEOUT_MS=120000

# Health poll interval for WireGuard interfaces. The image contains
# wireguard-tools, but the Docker host must provide the WireGuard kernel module.
WIREGUARD_HEALTH_CHECK_INTERVAL_MS=5000

# Disable by default: a Proton reset invalidates the account-wide OpenVPN
# credential pair and restarts every active tunnel.
RESET_CREDENTIALS_ON_START=false

# Private policy-routing identifiers for multi-tunnel isolation.
ROUTING_TABLE_BASE=10000
ROUTING_MARK_BASE=5898240
ROUTING_RULE_PRIORITY_BASE=12000

# Optional comma-separated resolver override for initial hostname resolution.
DNS_SERVERS_OVERRIDE=
