{
  "name": "proton-privoxy-app",
  "version": "1.0.0",
  "description": "Application logic for multi-tunnel OpenVPN with rotating proxy",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun build ./index.ts --outdir ./dist --target bun",
    "start": "bun run ./index.ts",
    "preinstall": "cd ../tundialer-native && npm install"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "patchright": "^1.52.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.14.10"
  }
}
