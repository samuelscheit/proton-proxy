{
  "name": "proton-privoxy-app",
  "version": "1.0.0",
  "description": "Application logic for multi-tunnel OpenVPN with sticky rotating proxy sessions",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test ./tunnel_profiles_test.ts ./session_affinity_test.ts",
    "start": "node ./index.ts",
    "preinstall": "cd ../tundialer-native && npm ci"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "patchright": "^1.52.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "5.9.3"
  }
}
